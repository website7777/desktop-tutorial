<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Только безопасные разрешения -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Keyboard3">

        <!-- Главная активность для настроек -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="Keyboard3">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Сервис клавиатуры -->
        <service
            android:name=".CustomKeyboard"
            android:permission="android.permission.BIND_INPUT_METHOD"
            android:exported="true"
            android:label="Keyboard3">
            <intent-filter>
                <action android:name="android.view.InputMethod" />
            </intent-filter>
            <meta-data
                android:name="android.view.im"
                android:resource="@xml/method" />
        </service>

        <!-- Сервис синхронизации буфера обмена -->
        <service
            android:name=".ClipboardSyncService"
            android:enabled="true"
            android:exported="false" />

    </application>

</manifest>